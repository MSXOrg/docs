# CLAUDE.md

@AGENTS.md

Claude Code does not read `AGENTS.md` automatically, so this file imports it — the line above pulls in the shared instructions every agent follows. Keep any Claude-specific notes below the import; everything else lives in [AGENTS.md](AGENTS.md) and the documentation it points to.
